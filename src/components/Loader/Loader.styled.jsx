import ClipLoader from 'react-spinners/ClipLoader';
import styled from 'styled-components';

export const LoaderStyled = styled(ClipLoader)`
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
